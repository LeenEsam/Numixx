/*export default function Sidebar({ lessons, onSelectLesson }) {
  return (
    <aside className="w-64 bg-gray-100 p-4 h-full overflow-y-auto">
      <h2 className="font-semibold mb-2">الدروس</h2>
      <ul>
        {lessons.map((lesson) => (
          <li key={lesson.id}>
            <button
              className="w-full text-left p-2 hover:bg-gray-200 rounded"
              onClick={() => onSelectLesson(lesson)}
            >
              {lesson.title}
            </button>
          </li>
        ))}
      </ul>
    </aside>
  );
}*/
